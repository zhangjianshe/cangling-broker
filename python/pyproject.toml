[project]
name = "cangling-message"
version = "0.1.4"
description = "Python producer and consumer for the cangling-message broker"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "zhangjianshe", email = "zhangjianshe@gmail.com" }]
license = { text = "Apache-2.0" }
dependencies = [
    "grpcio>=1.83.0",
    "protobuf>=5.28.0",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
]
urls.Homepage = "https://github.com/zhangjianshe/cangling-message"
urls.Repository = "https://github.com/zhangjianshe/cangling-message"

[project.optional-dependencies]
dev = ["grpcio-tools>=1.83.0", "build", "twine"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["cangling_message", "cangling_message.*"]
